name: Step 3

on:
  push:
    branches:
      - add-issue-templates
    paths:
      - ".github/ISSUE_TEMPLATE/**"
      - "CODE_OF_CONDUCT.md"

permissions:
  contents: read
  actions: write
  issues: write

env:
  STEP_4_FILE: ".github/steps/4-prepare-for-the-inevitable.md"

jobs:
  find_exercise:
    name: Find Exercise Issue
    uses: skills/exercise-toolkit/.github/workflows/find-exercise-issue.yml@v0.3.0

  check_step_work:
    name: Check step work
    runs-on: ubuntu-latest
    needs: [find_exercise]
    env:
      ISSUE_URL: ${{ needs.find_exercise.outputs.issue-url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get response templates
        uses: actions/checkout@v4
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.3.0

      - name: Update comment - checking work
        run: |
          gh issue comment "$ISSUE_URL" \
            --body-file exercise-toolkit/markdown-templates/step-feedback/checking-work.md \
            --edit-last
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # START: Check practical exercise
      - name: Check for Code of Conduct
        id: check-code-of-conduct
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            const fs = require('fs');

            // Result object to store the message
            let result = {
              passed: true
              description: ''
            }

            // Check that file exists
            if (!fs.existsSync('CODE_OF_CONDUCT.md')) {
              result.passed = false;
              result.description = 'File is missing.';
            }

            core.setOutput('result', result);

      - name: Check for Bug Report Templates
        id: check-bug-report-template
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            const fs = require('fs');

            // Result object to store the message
            let result = {
              passed: true
              description: ''
            }

            // Check that file exists
            if (!fs.existsSync('.github/ISSUE_TEMPLATE/bug_report.md')) {
              result.passed = false;
              result.description = 'File is missing.';
            }

            core.setOutput('result', result);

      - name: Check for Feature Request Templates
        id: check-feature-request-template
        uses: actions/github-script@v7
        continue-on-error: true
        with:
          script: |
            const fs = require('fs');

            // Result object to store the message
            let result = {
              passed: true
              description: ''
            }

            // Check that file exists
            if (!fs.existsSync('.github/ISSUE_TEMPLATE/feature_request.md')) {
              result.passed = false;
              result.description = 'File is missing.';
            }

            core.setOutput('result', result);

      - name: Build message - step results
        id: build-message-step-results
        uses: skills/action-text-variables@v2
        with:
          template-file: exercise-toolkit/markdown-templates/step-feedback/step-results.md
          template-vars: >
            {
              "step_number": 4,
              "passed": ${{ steps.check-code-of-conduct.outcome == 'success' && steps.check-bug-report-template.outcome == 'success' && steps.check-feature-request-template.outcome == 'success' }},
              "results_table": [
                  ${{ steps.check-code-of-conduct.outputs.result }},
                  ${{ steps.check-bug-report-template.outputs.result }},
                  ${{ steps.check-feature-request-template.outputs.result }}
                ]
            }

      - name: Create comment - step results
        run: |
          gh issue comment "$ISSUE_URL" \
            --body "$COMMENT_BODY" \
            --edit-last
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMENT_BODY: ${{ steps.build-message-step-results.outputs.updated-text }}

      - name: Fail job if not all checks passed
        if: steps.check-code-of-conduct.outcome == 'failure' || steps.check-bug-report-template.outcome == 'failure' || steps.check-feature-request-template.outcome == 'failure'
        run: exit 1

      # END: Check practical exercise

      - name: Build message - step finished
        id: build-message-step-finish
        uses: skills/action-text-variables@v2
        with:
          template-file: exercise-toolkit/markdown-templates/step-feedback/step-finished-prepare-next-step.md
          template-vars: |
            next_step_number: 4

      - name: Update comment - step status
        run: |
          gh issue comment "$ISSUE_URL" \
            --body "$ISSUE_BODY" \
            --edit-last
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_BODY: ${{ steps.build-message-step-finish.outputs.updated-text }}

  post_next_step_content:
    name: Post next step content
    needs: [find_exercise, check_step_work]
    runs-on: ubuntu-latest
    env:
      ISSUE_URL: ${{ needs.find_exercise.outputs.issue-url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get response templates
        uses: actions/checkout@v4
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.3.0

      - name: Create comment - add step content
        run: |
          gh issue comment "$ISSUE_URL" \
            --body-file "$STEP_4_FILE"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create comment - watching for progress
        run: |
          gh issue comment "$ISSUE_URL" \
            --body-file exercise-toolkit/markdown-templates/step-feedback/watching-for-progress.md
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Disable current workflow and enable next one
        run: |
          gh workflow disable "Step 3"
          gh workflow enable "Step 4"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
